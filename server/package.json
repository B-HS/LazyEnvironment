{
    "name": "lazy-environment-server",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "module": "src/index.ts",
    "scripts": {
        "dev": "bun run --watch src/index.ts",
        "start": "bun run src/index.ts",
        "test": "bun test",
        "typecheck": "bunx tsc --noEmit",
        "db:generate": "drizzle-kit generate",
        "db:migrate": "drizzle-kit migrate",
        "vercel-build": "bun build ./app.ts --outfile ./api/hub.js --target bun --format esm --external @libsql/client --external libsql && bun run db:migrate"
    },
    "dependencies": {
        "@libsql/client": "0.17.4",
        "drizzle-orm": "0.45.2",
        "hono": "4.12.30",
        "zod": "4.4.3"
    },
    "devDependencies": {
        "@types/bun": "1.3.14",
        "drizzle-kit": "0.31.10",
        "typescript": "5.9.3"
    }
}
